import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: "#e56324",
    colorPrimaryBg: "#ccc",
    borderRadius: 4,
  },
};

export default theme;
